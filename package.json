{
  "name": "resume-builder-server",
  "version": "1.0.0",
  "description": "Groq proxy server for Resume Builder",
  "main": "server.js",
  "scripts": {
    "build": "npm --prefix resume-builder run build",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
